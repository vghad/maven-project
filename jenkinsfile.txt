pipeline{
	agent any

	tools{
		maven 'localmaven'
	}

      stages{
			
		stage('Docker Pull'){
				steps{
					echo "Switch to user ansible"
					sh "sudo su ansible"
					echo "Pull docker image form dockerhub"
					sh "sudo ansible-playbook deploy-test.yml"
					echo "Docker image deployed on Node successfully ....!!!"
				
					}
				}
	}
}		
