pipeline{
	agent any

	tools{
		maven 'localmaven'
	}

      stages{
			
		stage('Docker Pull'){
				steps{
					echo "Check Ansible syntax"
					sh "ansible-playbook deploy-test.yml --syntax-check"
					echo "Switch to user ansible"
					sh "sudo login -f ansible"
					echo "Pull docker image form dockerhub"
					sh "ansible-playbook deploy-test.yml"
					echo "Docker image deployed on Node successfully ....!!!"
				
					}
				}
	}
}		
